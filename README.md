# BibleBubby App Store legal pages

Source for the public GitHub Pages paths used in App Store Connect:

- https://duckflow-tools.github.io/biblebubby-site/privacy
- https://duckflow-tools.github.io/biblebubby-site/support

Copy matches `src/domain/billing.ts` (email, lesson progress, subscription state, Sentry crash and performance data linked to the account identifier when signed in, App Store build does not send answers to a language model; the app does not serve advertisements). DNS1 can attach a custom domain later. Do not add a CNAME until that hostname resolves.
